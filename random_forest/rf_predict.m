% rf_predict(forest_ptr, data_mtx)
%  Uses the forest pointed at by forest_ptr to predict values for everything in data_mtx.
%  data_mtx must have one row per sample (one column per feature) as with training.